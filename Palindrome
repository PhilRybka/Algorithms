#include <iostream>
#include <algorithm>
using namespace std;

bool palindrome(string a){
    string temp=a;
    reverse(temp.begin(),temp.end());
    if(temp==a) return true;
    return false;
}

int main(){

string a;
cin>>a;
cout<<palindrome(a);

}
