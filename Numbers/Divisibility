#include <iostream>
using namespace std;

bool divisible(int a,int b){
    if(a%b==0) return true;
    return false;
}

int main(){
  
  int a,b;
  cin>>a>>b;
  cout<<divisible(a,b);  
  return 0;
  }
  
