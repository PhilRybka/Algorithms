#include <iostream>
using namespace std;

bool isPrime(int n){
    for(int i=2;i*i<=n;i++){				//optimalized version, no need to loop until n
        if(n%i==0) return false;
    }
    return true;
}

int main(){
  
  int n;
  cin>>n;
  cout<<isPrime(n;
  return 0;
  
  }
